allOf:
  - $ref: '#/components/schemas/FaucetKey'
  - type: object
    required:
      - attributes
    properties:
      attributes:
        type: object
        required:
          - recipient
        properties:
          recipient:
            $ref: '#/components/schemas/Recipient'